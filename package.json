{
  "name": "bankin",
  "version": "0.0.1",
  "description": "",
  "keywords": [
    "bankin",
    "bank",
    "banking"
  ],
  "author": "Lendix IFP",
  "contributors": "https://github.com/Lendix/bankin/graphs/contributors",
  "bugs": "https://github.com/Lendix/bankin/issues",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && babel index.js --out-file dist/index.js && babel lib --out-dir dist/lib",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "request": "^2.75.0",
    "request-promise": "^4.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0"
  }
}
